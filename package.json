{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix apps/web\" \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/pocketbase\"",
    "build": "npm run build --prefix apps/web && npm run build --prefix apps/api && node tools/deploy-copy.js",
    "start": "concurrently --raw \"npm run start --prefix apps/api\" \"npm run start --prefix apps/pocketbase\"",
    "lint": "concurrently --raw \"npm run lint --prefix apps/web\" \"npm run lint --prefix apps/api\"",
    "postinstall": "chmod +x apps/pocketbase/pocketbase"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "workspaces": [
    "apps/*"
  ]
}